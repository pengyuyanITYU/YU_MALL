spring:
  application:
    name: yu-mall-comment-service # 服务名称
  cloud:
    nacos:
      server-addr: 192.168.100.128:8848 # nacos地址
      config:
        file-extension: yaml # 文件后缀名
        shared-configs: # 共享配置
          - dataId: shared-jdbc.yaml # 共享mybatis配置
          - dataId: shared-log.yaml # 共享日志配置
          - dataId: shared-swagger.yaml # 共享日志配置
          - dataId: shared-seata.yaml  # seata配置
          - dataId: shared-mq.yaml    # mq配置
          - dataId: shared-feign.yaml # feign配置